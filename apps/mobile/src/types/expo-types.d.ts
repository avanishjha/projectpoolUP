// Pulls in Expo's ambient types (process.env.EXPO_PUBLIC_*, asset modules).
// Lives here (not expo-env.d.ts) because the Expo CLI deletes/regenerates
// that root file as it pleases.
/// <reference types="expo/types" />
