# Expo SDK version

This app is pinned to **Expo SDK 54** (matches the team's Expo Go). Read the exact
versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.
Upgrade path: move to a development build (expo-dev-client, already a dependency)
when the first custom native module lands (Razorpay, F5.x) — then re-evaluate SDK.
